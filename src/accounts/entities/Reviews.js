export default class {
    constructor(movieId, author, review, rating) {
        this.movieId = movieId;
        this.author = author;
        this.review = review;
        this.rating = rating;
    }
}